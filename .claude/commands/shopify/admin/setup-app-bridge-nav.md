Setup App Bridge Navigation
Configure App Bridge navigation for admin panels.
## Requirements:

Use @shopify/app-bridge-react
Follow https://shopify.dev/docs/api/app-bridge patterns
Configure proper routing
Handle embedded context
Add loading states

Key Components:

Provider configuration
Navigation setup
Session token handling
Error boundaries

Navigation type: $ARGUMENTS